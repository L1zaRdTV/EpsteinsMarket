<Page x:Class="EpsteinMarket.Pages.AutorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EpsteinMarket.Pages"
      mc:Ignorable="d"
      d:DesignHeight="700"
      d:DesignWidth="1100"
      Title="AutorizationPage">

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Border Style="{StaticResource CardBorderStyle}"
                Width="400"
                MaxWidth="400"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="24">
            <StackPanel>
                <TextBlock Text="EpsteinsMarket"
                           Foreground="{StaticResource AccentTextBrush}"
                           FontSize="34"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>

                <TextBlock Text="Login:" FontSize="15"/>
                <TextBox x:Name="txtLogin" MaxLength="50"/>

                <TextBlock Text="Password:" FontSize="15"/>
                <PasswordBox x:Name="txtPassword" MaxLength="50"/>

                <Button x:Name="btnLogin"
                        Content="Войти"
                        Click="btnLogin_Click"
                        Margin="0,8,0,6"/>
                <Button x:Name="btnReg"
                        Content="Регистрация"
                        Click="btnReg_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
