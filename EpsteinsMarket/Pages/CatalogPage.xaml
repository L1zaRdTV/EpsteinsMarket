<Page x:Class="EpsteinMarket.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EpsteinMarket.Pages"
      mc:Ignorable="d"
      d:DesignHeight="700"
      d:DesignWidth="1100"
      Title="CatalogPage">

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBackgroundBrush}" CornerRadius="0,0,14,14" Padding="16,12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Каталог товаров" Style="{StaticResource PageHeaderTextStyle}" Grid.Column="0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnAdmin" Content="Админ-панель" Click="btnAdmin_Click"/>
                    <Button x:Name="BtnCart" Content="Корзина" Click="BtnCart_Click"/>
                    <Button x:Name="btnExit" Content="Выйти" Click="btnExit_Click"/>
                    <Button x:Name="btnProfile"
                            Width="46"
                            Padding="0"
                            Click="btnProfile_Click"
                            BorderBrush="{StaticResource BorderBrushSoft}">
                        <Button.Background>
                            <ImageBrush ImageSource="/Pages/free-icon-user-847969.png"/>
                        </Button.Background>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="12,0,12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock x:Name="tbWelcome"
                               Text="Добро пожаловать"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{StaticResource AccentTextBrush}"/>
                    <TextBlock x:Name="tbRole"
                               Text="Роль:"
                               Margin="0,6,0,0"
                               FontSize="16"
                               Foreground="{StaticResource AccentTextBrush}"/>
                </StackPanel>

                <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                    <StackPanel Width="170" Margin="4,0">
                        <TextBlock Text="Сортировка" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <ComboBox x:Name="cmbSort" SelectionChanged="cmbSort_SelectionChanged"/>
                    </StackPanel>
                    <StackPanel Width="170" Margin="4,0">
                        <TextBlock Text="Категория" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <ComboBox x:Name="cmbCategory" SelectionChanged="cmbCategory_SelectionChanged"/>
                    </StackPanel>
                    <StackPanel Width="200" Margin="4,0">
                        <TextBlock Text="Поиск" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtSearch" TextChanged="txtSearch_TextChanged"/>
                    </StackPanel>
                </WrapPanel>
            </Grid>
        </Border>

        <ListView x:Name="lvProducts" Grid.Row="2" Margin="12,0,12,12" BorderThickness="0" Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" Margin="0,0,10,0" CornerRadius="8">
                                <Image Source="{Binding ImagePath}" Height="120" Stretch="UniformToFill"/>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding ProductName}" FontSize="18" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Price, StringFormat=Цена: {0} ₽}" Margin="0,8,0,0" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding QuantityInStock, StringFormat=Остаток: {0}}" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Description}" Margin="0,8,0,0" TextWrapping="Wrap" MaxHeight="72" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <Button Content="В корзину" Tag="{Binding SourceProduct}" Margin="0,0,0,8" Click="btnAddToCart_Click" Style="{StaticResource PrimaryButtonStyle}"/>
                                <Button Content="Подробнее" Tag="{Binding SourceProduct}" Click="btnDetails_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
