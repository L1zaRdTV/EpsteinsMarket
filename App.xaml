<Application x:Class="EpsteinsMarket.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EpsteinsMarket"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Color x:Key="OceanBlue">#176BB3</Color>
        <Color x:Key="DeepOceanBlue">#0E4E85</Color>
        <Color x:Key="FoamWhite">#F8FCFF</Color>

        <LinearGradientBrush x:Key="MainBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#D5EEFF" Offset="0"/>
            <GradientStop Color="#9CD2FA" Offset="0.55"/>
            <GradientStop Color="#75B9EC" Offset="1"/>
        </LinearGradientBrush>

        <VisualBrush x:Key="StripeOverlayBrush"
                     TileMode="Tile"
                     Viewport="0,0,22,22"
                     ViewportUnits="Absolute"
                     Viewbox="0,0,22,22"
                     ViewboxUnits="Absolute"
                     Opacity="0.2"
                     Stretch="None">
            <VisualBrush.Visual>
                <Grid Width="22" Height="22">
                    <Rectangle Fill="#00FFFFFF"/>
                    <Rectangle Fill="#FFFFFFFF" Width="10" HorizontalAlignment="Left"/>
                </Grid>
            </VisualBrush.Visual>
        </VisualBrush>

        <Style TargetType="Page">
            <Setter Property="Background" Value="{StaticResource MainBackgroundBrush}"/>
            <Setter Property="TextElement.Foreground" Value="#05315B"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#EEFFFFFF"/>
            <Setter Property="BorderBrush" Value="#90FFFFFF"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{StaticResource OceanBlue}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource DeepOceanBlue}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="{StaticResource FoamWhite}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="BorderBrush" Value="#8BC0E8"/>
            <Setter Property="Foreground" Value="#05315B"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="{StaticResource FoamWhite}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="BorderBrush" Value="#8BC0E8"/>
            <Setter Property="Foreground" Value="#05315B"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="{StaticResource FoamWhite}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="BorderBrush" Value="#8BC0E8"/>
        </Style>
    </Application.Resources>
</Application>
