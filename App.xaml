<Application x:Class="EpsteinsMarket.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EpsteinsMarket"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="OceanBlue" Color="#0A5FA8"/>
        <SolidColorBrush x:Key="DeepOceanBlue" Color="#07406E"/>
        <SolidColorBrush x:Key="SkyBlue" Color="#DDF1FF"/>
        <SolidColorBrush x:Key="FoamWhite" Color="#F9FDFF"/>
        <SolidColorBrush x:Key="PalmLeaf" Color="#2A8C6D"/>
        <SolidColorBrush x:Key="SandTone" Color="#F5E7CA"/>

        <LinearGradientBrush x:Key="MainBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#EAF7FF" Offset="0"/>
            <GradientStop Color="#B7E3FF" Offset="0.38"/>
            <GradientStop Color="#5EB4EA" Offset="0.78"/>
            <GradientStop Color="#1D7BB9" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AccentWaveBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#D7EEFF" Offset="0.4"/>
            <GradientStop Color="#B6E0FF" Offset="0.7"/>
            <GradientStop Color="#FFFFFF" Offset="1"/>
        </LinearGradientBrush>

        <VisualBrush x:Key="StripeOverlayBrush"
                     TileMode="Tile"
                     Viewport="0,0,44,44"
                     ViewportUnits="Absolute"
                     Viewbox="0,0,44,44"
                     ViewboxUnits="Absolute"
                     Opacity="0.48"
                     Stretch="None">
            <VisualBrush.Visual>
                <Grid Width="44" Height="44" Background="#00FFFFFF">
                    <Rectangle Fill="#14FFFFFF" Width="44" Height="44"/>
                    <Rectangle Fill="#32FFFFFF" Width="44" Height="13" VerticalAlignment="Top"/>
                    <Rectangle Fill="#24BCE3FF" Width="44" Height="9" Margin="0,18,0,0" VerticalAlignment="Top"/>
                    <Rectangle Fill="#36FFFFFF" Width="44" Height="8" Margin="0,32,0,0" VerticalAlignment="Top"/>
                </Grid>
            </VisualBrush.Visual>
        </VisualBrush>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource MainBackgroundBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextElement.Foreground" Value="#04355E"/>
        </Style>

        <Style TargetType="Page">
            <Setter Property="Background" Value="{StaticResource MainBackgroundBrush}"/>
            <Setter Property="TextElement.Foreground" Value="#04355E"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#EEFFFFFF"/>
            <Setter Property="BorderBrush" Value="#9CCBEC"/>
            <Setter Property="BorderThickness" Value="1.6"/>
            <Setter Property="CornerRadius" Value="22"/>
            <Setter Property="Padding" Value="22"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#3A0B4A7D"
                                      BlurRadius="22"
                                      ShadowDepth="7"
                                      Direction="270"
                                      Opacity="0.55"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{StaticResource OceanBlue}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource DeepOceanBlue}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.84"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Background" Value="{StaticResource FoamWhite}"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="BorderBrush" Value="#8CBFE3"/>
            <Setter Property="Foreground" Value="#04355E"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#0A5FA8"/>
                    <Setter Property="Background" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Background" Value="{StaticResource FoamWhite}"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="BorderBrush" Value="#8CBFE3"/>
            <Setter Property="Foreground" Value="#04355E"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#0A5FA8"/>
                    <Setter Property="Background" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="{StaticResource FoamWhite}"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="BorderBrush" Value="#8CBFE3"/>
        </Style>

        <Style x:Key="HeroTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="44"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#70063F68" BlurRadius="10" ShadowDepth="3" Opacity="0.9"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
