<Page x:Class="EpsteinsMarket.Pages.Auth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Auth">
    <Grid>
        <Grid Margin="24" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="960">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center" Margin="0,0,28,0">
                <TextBlock Text="Epsteins Market" FontSize="36" FontWeight="Bold"/>
                <TextBlock Text="Минималистичный интерфейс для авторизации, каталога и управления товарами."
                           Margin="0,12,0,0"
                           FontSize="16"
                           Foreground="{StaticResource TextMuted}"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Вход" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Grid.Row="1" Margin="0,10,0,0" Text="Логин" FontWeight="SemiBold"/>
                    <TextBox x:Name="tbLogin" Grid.Row="2" Margin="0,6,0,0" TextChanged="tbLogin_TextChanged"/>
                    <TextBlock Grid.Row="3" Margin="0,12,0,0" Text="Пароль" FontWeight="SemiBold"/>
                    <PasswordBox x:Name="pbPassword" Grid.Row="4" Margin="0,6,0,0" PasswordChanged="pbPassword_PasswordChanged"/>

                    <StackPanel Grid.Row="5" Margin="0,18,0,0">
                        <Button x:Name="btnLogin" Height="36" Content="Войти" Click="btnLogin_Click"/>
                        <Button x:Name="btnReg" Margin="0,8,0,0" Height="36" Background="#374151" Content="Регистрация" Click="btnReg_Click"/>
                        <TextBlock x:Name="tbStatus" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#B91C1C"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
